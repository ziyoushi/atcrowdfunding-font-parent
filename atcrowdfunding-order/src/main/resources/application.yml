server:
  port: 7000

spring:
  application:
    name: atcrowdfunding-order
  redis:
    host: 192.168.183.150

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
